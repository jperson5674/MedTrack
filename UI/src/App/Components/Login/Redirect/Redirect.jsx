import LoginTemplate from "../../LoginTemplate";


const Redirect = function Redirect(backgroundImageUrl){

    return(
        <LoginTemplate backgroundImageUrl={backgroundImageUrl}></LoginTemplate>
    );
};

export default Redirect;